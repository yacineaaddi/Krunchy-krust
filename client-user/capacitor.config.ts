import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.krunchykrust.userdash',
  appName: 'Krunchy Krust',
  webDir: 'dist'
};

export default config;
